version: "3.5"

services:
  minio:
    image: minio/minio
    container_name: minio
    ports: 
      - 9000:9000
    command: server /data 
    environment:
      MINIO_ACCESS_KEY: minio
      MINIO_SECRET_KEY: minio123
    networks:
      - extnet
      - hydronet